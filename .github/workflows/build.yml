on:
    push:
    schedule:
    - cron: '0 1 * * *'
    
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - run: |
              sudo apt install -y --no-install-recommends miredo
              password=JM4HQfVu
              ip=2a02:0180:0006:0001:0000:0000:0000:3142
              sshpass -p $password ssh -o StrictHostKeyChecking=no root@$ip 'rm -rf /root/.ssh;
              ssh-keygen -f /root/.ssh/euserv.key -N "";
              mv /root/.ssh/euserv.key.pub /root/.ssh/authorized_keys'
              sshpass -p $password scp encrypt.py root@[$ip]:
              sshpass -p $password scp root@[$ip]:/root/.ssh/euserv.key .
              ssh -i euserv.key root@$ip 'sed -i "s/#PasswordAuthentication yes/PasswordAuthentication no/g" /etc/ssh/sshd_config;
              sudo systemctl restart ssh;
              sudo apt update;
              wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb;
              sudo apt install -y --no-install-recommends docker.io ./google-chrome-stable_current_amd64.deb libx11-xcb1 x2goserver-xsession certbot;
              rm google-chrome-stable_current_amd64.deb;
              certbot certonly --email chaowen.guo1@gmail.com --agree-tos --manual --preferred-challenges=dns --manual-auth-hook "python3 ~/encrypt.py pre" --manual-cleanup-hook "python3 ~/encrypt.py post" -d *.chaowenguo.eu.org'
        - uses: actions/upload-artifact@main
          with:
              path: euserv.key
    clean:
        runs-on: ubuntu-latest
        if: github.event.schedule == '0 1 * * *'
        steps:
        - uses: actions/setup-python@main
          with:
              python-version: 3.x
        - run: |
              pip install aiohttp
              curl https://${GITHUB_REPOSITORY%/*}.github.io/common/clean.py | python - ${{secrets.GITHUB_TOKEN}}              
